using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pavel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(DataManager.Instance.hp * 3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
