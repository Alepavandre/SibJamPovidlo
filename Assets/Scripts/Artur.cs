using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Artur : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(DataManager.Instance.time);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
